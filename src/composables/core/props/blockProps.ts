const blockProps = {
    block: {
        type: Boolean,
        default: false,
        description: "Make the component take up the full width of its container",
    },
};

export default blockProps;
